/**
 * Utility to parse and format image and video URLs.
 * Re-exported from googleBackendBridge to ensure universal availability.
 */
export {
  isActualMediaUrl,
  extractGoogleDriveId,
  formatImageUrl,
  isVideoUrl,
} from "./googleBackendBridge";
